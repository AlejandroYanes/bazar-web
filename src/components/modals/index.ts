export enum Modals {
  UPDATES = '#updates',
  FILTERS = '#filters',
  INVITE = '#invite',
}

export const modalsMap = {
  [Modals.INVITE]: null,
  [Modals.FILTERS]: null,
  [Modals.UPDATES]: null,
};
